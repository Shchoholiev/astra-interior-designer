# Keep the shared backend token server-side; never prefix it with NEXT_PUBLIC_.
ASTRA_API_URL=http://127.0.0.1:8000
ASTRA_API_KEY=

# Leave false to run the self-contained UI demo without AWS/Modal.
NEXT_PUBLIC_ASTRA_BACKEND_ENABLED=false
